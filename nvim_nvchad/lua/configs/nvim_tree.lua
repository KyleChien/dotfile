local nvim_tree = require("nvim-tree")

-- nvim_tree.setup({
--     -- sync_root_with_cwd = true, -- Keeps the tree in sync with the current working directory
--     -- update_focused_file = {
--     --     enable = true, -- Updates the tree to focus on the current file
--     --     update_root = true, -- Updates the tree's root directory
--     -- },
--     -- respect_buf_cwd = true, -- Respects the buffer's current directory
-- })

return nvim_tree
